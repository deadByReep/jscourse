/*function get_name_browser(){

    var name = navigator.userAgent;    
    
    if (name.search(/Chrome/) > 0) {
        alert('Chrome')
    };
    if (name.search(/Firefox/) > 0) {
        alert('Firefox');
    };

    if (name.search(/IE/) > 0) {
        alert('IE');
    };
    return 'Не определен';
};
 
get_name_browser();*/
alert(window.navigator.userAgent);